import Test.@test

# test items
# 1. Context has 1) constant information, 2) accel type, 3) accelerator used, and other task context
# 2. enterdata, exitdata: data movement in unstructured way per context
# 3. offload: launches a kernel per context

@test true
